{{- if .Values.bootstrap.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "control-layer.fullname" . }}-dashboard-bootstrap
  labels:
    {{- include "control-layer.labels" . | nindent 4 }}
data:
  bootstrap.js: {{ .Values.bootstrap.content | b64enc }}
{{- end }}
